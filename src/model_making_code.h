#include <cmath>
#include <cstddef>
#include <vector>

#include "primitiveDatatypes.h"

shape_data make_uv_sphere_data(size_t sectorCount, size_t stackCount);
shape_indices make_uv_sphere_indices(size_t sectorCount, size_t stackCount);
